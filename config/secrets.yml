# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

shared:
   supervision_api_url: 'https://librenms.mbx.dr/api/v0/devices'
   snmp_auth_name: 'PetroLan'
   snmp_auth_pass: 'P0rtM4n3'

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 48492ec511c5089f8bcaf3cfaa89911afba62e3d0796cb8b986fa9f75d8e5e868cef2e71d62fc07d87d62abe379882e34e65d4304c03c8c261ae94e30bce5828

test:
  secret_key_base: 71dda143ca4cc82c605f5cb24df8fbaa15524aa68c67ad53e5ccf340b1449903671df7f906d228fe18d0f9239e9257416ac6b00896c56fd35d9d109b7b85cd87

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
