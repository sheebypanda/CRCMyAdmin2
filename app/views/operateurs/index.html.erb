<% if notice  %>
  <div class="alert alert-success animated slideInDown" role="alert">
    <%= notice %>
  </div>
<% end %>

<%= link_to fa_icon("plus")+' Ajouter un opérateur', new_operateur_path, class: 'btn btn-outline-primary' %>
<table>
  <thead>
    <tr>
      <th>Identifiant</th>
      <th>Nom</th>
      <th>Hotline</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @operateurs.each do |operateur| %>
      <tr>
        <td><%= operateur.id %></td>
        <td><%= operateur.nom %></td>
        <td><%= operateur.hotline %></td>
        <td><%= link_to 'Show', operateur %></td>
        <td><%= link_to 'Edit', edit_operateur_path(operateur) %></td>
        <td><%= link_to 'Destroy', operateur, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
