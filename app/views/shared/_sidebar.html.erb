<nav class="col-md-2 d-none d-md-block bg-light sidebar p-0">

  <div class="sidebar-sticky p-0">
    <div class="list-group list-group-flush">
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if current_page?(root_url) %>active<% end %>" href="<%= root_path %>">
        <%= fa_icon("dashboard 2x") %> Tableau de bord <span></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/recettes') %>active<% end %>" href="<%= recettes_path %>">
        <%= fa_icon("clipboard 2x") %> Fiches recettes <span class="badge badge-pill <% if request.path.start_with?('/recettes') %>badge-light<% else %>badge-success<% end %>"><%= Recette.all.count %></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/equipements') %>active<% end %>" href="<%= equipements_path %>">
        <%= fa_icon("server 2x") %> Équipements <span class="badge badge-pill <% if request.path.start_with?('/equipements') %>badge-light<% else %>badge-danger<% end %>"><%= Equipement.all.count %></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/stock') %>active<% end %>" href="/stock">
        <%= fa_icon("truck 2x") %> Stock <span class="badge badge-pill <% if request.path.start_with?('/stock') %>badge-light<% else %>badge-warning<% end %>"><%= Equipement.where(nom: nil).or(Equipement.where(nom: '')).count %></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/projets') %>active<% end %>" href="/projets">
        <%= fa_icon("briefcase 2x") %> Projets <span class="badge badge-pill <% if request.path.start_with?('/projets') %>badge-light<% else %>badge-primary<% end %>"><%= Projet.all.count %></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/lignes') %>active<% end %>" href="<%= lignes_path %>">
        <%= fa_icon("phone 2x") %> Lignes <span class="badge badge-pill <% if request.path.start_with?('/lignes') %>badge-light<% else %>badge-purple<% end %>"><%= Ligne.all.count %></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/localisations') %>active<% end %>" href="<%= localisations_path %>">
        <%= fa_icon("map-marker 2x") %> Localisations <span class="badge badge-pill <% if request.path.start_with?('/localisations') %>badge-light<% else %>badge-info<% end %>"><%= Localisation.all.count %></span>
      </a>
      <a class="list-group-item list-group-item-action d-flex justify-content-between align-items-center <% if request.path.start_with?('/operateurs') %>active<% end %>" href="<%= operateurs_path %>">
        <%= fa_icon("user-secret 2x") %> Opérateurs <span class="badge badge-pill <% if request.path.start_with?('/operateurs') %>badge-light<% else %>badge-primary<% end %>"><%= Operateur.all.count %></span>
      </a>


      <div class="dropright">
        <a class="list-group-item list-group-item-action list-group-item dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= fa_icon("table")%> Exports CSV
        </a>
        <div class="dropdown-menu">
          <%= link_to fa_icon("table", class: "text-danger")+' Équipements', equipements_path(format: :csv), class: 'list-group-item list-group-item-action text-danger'%>
          <%= link_to fa_icon("table", class: "text-warning")+' Lignes', lignes_path(format: :csv), class: 'list-group-item list-group-item-action text-warning'%>
          <%= link_to fa_icon("table", class: "text-info")+' Localisations', localisations_path(format: :csv), class: 'list-group-item list-group-item-action text-info'%>
          <%= link_to fa_icon("table", class: "text-purple")+' État du stock', '/stock.csv', format: :csv, class: 'list-group-item list-group-item-action text-purple'%>
        </div>
      </div>
      <!-- <div class="dropright">
        <a class="list-group-item list-group-item-action list-group-item dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= fa_icon("file-excel-o")%> Exports Excel
        </a>
        <div class="dropdown-menu">
          <%= link_to fa_icon("file-excel-o")+' Équipements', equipements_path(format: :xls), class: 'list-group-item list-group-item-action'%>
          <%= link_to fa_icon("file-excel-o")+' Lignes', lignes_path(format: :xls), class: 'list-group-item list-group-item-action'%>
          <%= link_to fa_icon("file-excel-o")+' Localisations', localisations_path(format: :xls), class: 'list-group-item list-group-item-action'%>
          <%= link_to fa_icon("file-excel-o")+' État du stock', '/stock.csv', format: :xls, class: 'list-group-item list-group-item-action'%>
        </div>
      </div>-->
      <!-- <%= link_to fa_icon("refresh")+' Maj Serials', '/serials_update', class: 'list-group-item list-group-item-action'%> -->
      <!-- <%= link_to fa_icon("plus")+' Add hosts to LibreNMS', '/hosts_update', class: 'list-group-item list-group-item-action'%> -->
    </div>
  </div>
</nav>
