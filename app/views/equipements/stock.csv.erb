<%- headers = ['marque', 'modele', 'serial', 'reference_livraison'] -%>
<%= CSV.generate_line headers %>
<%- @stock_all.each do |s| -%>
  <% if s.livraison
      livraison = s.livraison.nom
    else
      livraison = ''
   end %>
  <%= CSV.generate_line([s.marque, s.modele, s.serial,livraison]).html_safe -%>
<%- end -%>
